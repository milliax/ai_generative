[project]
name = "ai-generative"
version = "0.1.0"
description = "Multi-agent green supply chain coordination system"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py311"
